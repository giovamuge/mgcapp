<?xml version="1.0" encoding="utf-8"?>

<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="Mugelli.Software.It.Mgc.RootPage"
            Title="{Binding Title}"
            BindingContext="{Binding RootViewModel, Source={StaticResource ViewModelLocator}}"
            Children="{Binding Childrens}">

    <!--Children="{Binding Tabs}"
            SelectedItem="{Binding CurrentTab, Mode=TwoWay}"-->

    <!--Pages can be added as references or inline-->
    <!--<ContentPage Title="{Binding TitleNews}" Content="{Binding News}" />
    <ContentPage Title="{Binding TitleCalendar}" Content="{Binding Calendar}" />
    <ContentPage Title="{Binding TitleCommunications}" Content="{Binding Communications}" />-->

</TabbedPage>