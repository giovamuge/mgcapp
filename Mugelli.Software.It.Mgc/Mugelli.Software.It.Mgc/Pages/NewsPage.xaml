<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:Mugelli.Software.It.Mgc.Behaviors;assembly=Mugelli.Software.It.Mgc"
             x:Class="Mugelli.Software.It.Mgc.Pages.NewsPage" 
             Title="{Binding Title}"
             Icon="{Binding Icon}"
             BindingContext="{Binding NewsViewModel, Source={StaticResource ViewModelLocator}}">
    <ContentPage.Content>
        <Grid>
            <Image Source="ColoredBackground" VerticalOptions="Fill" HorizontalOptions="Fill" Aspect="Fill"/>
            <ListView
                ItemsSource="{Binding NewsList}" 
                BackgroundColor="Transparent"
                RowHeight="200" 
                SeparatorVisibility="None"
                SelectedItem="{Binding ReadArticleCommand, Mode=TwoWay}">


                <ListView.Behaviors>
                    <!--<behaviors:ListViewSelectedItemBehavior Command="{Binding ReadArticleCommand}"
                                                            Converter="{StaticResource SelectedItemEventArgsToSelectedItemConverter}" />-->
                    <behaviors:EventToCommandBehavior Command="{Binding ReadArticleCommand}"
                                                      EventName="ItemSelected"
                                                      Converter="{StaticResource SelectedItemEventArgsToSelectedItemConverter}"/>
                </ListView.Behaviors>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid RowSpacing="5" ColumnSpacing="0" Margin="10,5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="AUTO" />
                                </Grid.RowDefinitions>

                                <Image Source="{Binding Path=HeroImage}" Aspect="AspectFill" Grid.RowSpan="2" />
                                <Image Source="ShadowOverlay" Grid.RowSpan="2" Aspect="Fill" VerticalOptions="End"
                                       HorizontalOptions="Fill" />

                                <Grid Margin="10" RowSpacing="10" ColumnSpacing="10" Grid.Row="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="AUTO" />
                                        <RowDefinition Height="25" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <!--<ColumnDefinition Width="25" />-->
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="AUTO" />
                                    </Grid.ColumnDefinitions>

                                    <Label Text="{Binding Path=ShortTitle}" Grid.Row="0" Grid.ColumnSpan="2"
                                           Style="{StaticResource TitleLabelSecondary}" LineBreakMode="NoWrap" />

                                    <!--<controls:CircleImage Source="{Binding ProfileImage}" Aspect="AspectFill" Grid.Row="1" Grid.Column="0" WidthRequest="25" HeightRequest="25" />-->
                                    <Label Text="{Binding Path=Subtitle}" Grid.Row="1" Grid.Column="0" FontSize="10"
                                           VerticalOptions="Center" Style="{StaticResource BodyLabelSecondary}" />

                                    <StackLayout Orientation="Horizontal" VerticalOptions="Center" Grid.Column="1"
                                                 Grid.Row="1" Spacing="5">
                                        <!-- TODO:aggiungere icona orologio -->
                                        <Image Source="Clock_50px.png" />
                                        <!--<Label Text="pubblicato" Style="{StaticResource BodyLabel}" />-->
                                        <Label Text="{Binding Path=DateCreate}"
                                               Style="{StaticResource HandleLabelSecondary}" />
                                    </StackLayout>
                                </Grid>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>